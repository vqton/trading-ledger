[pytest]
testpaths = accounting_app/tests
python_files = test_*.py e2e_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short
markers =
    unit: Unit tests (no database)
    integration: Integration tests (with database)
    e2e: End-to-end tests (Playwright browser)
